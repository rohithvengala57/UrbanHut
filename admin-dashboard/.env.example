# UrbanHut Admin Dashboard — environment variables
# Copy to .env.local and fill in values before running

# Base URL of the UrbanHut backend API (no trailing slash)
VITE_API_BASE_URL=http://localhost:8000/api/v1
